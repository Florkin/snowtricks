{% extends "base.html.twig" %}

{% block title %}
    {{ page.title }}
{% endblock %}

{% block body %}
    {% include "_partials/_page-header.html.twig" %}

    <div class="container">
        <div class="row">
            {% for trick in tricks %}
                {% include "_partials/_trick-miniature.html.twig" %}
            {% endfor %}
        </div>

        <div class="text-center">
            <div class="btn btn-primary js-load-more">Voir plus</div>
        </div>
    </div>


{% endblock %}